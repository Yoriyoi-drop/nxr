from setuptools import setup, find_packages

setup(
    name="nxr-client",
    version="0.1.0",
    description="NXR AI-native database Python SDK",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.8",
)
